// Superseded by include/lightning_lv_conf.h (selected via LV_CONF_PATH).
// LovyanGFX ships an example lv_conf.h that shadowed this filename on the
// library include path, so the config now uses a unique name. Do not put
// LVGL settings here.
